<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dwbi.android.popularmovies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.dwbi.android.popularmovies.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.dwbi.android.popularmovies.DetailActivity"
            android:label="@string/detail_activity_label"
            android:parentActivityName="com.dwbi.android.popularmovies.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.dwbi.android.popularmovies.MainActivity">
            </meta-data>
        </activity>

        <provider
            android:authorities="com.dwbi.android.popularmovies"
            android:name="com.dwbi.android.popularmovies.model.MovieProvider"
            android:exported="false">
        </provider>

    </application>

</manifest>